# LCPR 插件原理详解

## 📖 目录
1. [插件概述](#插件概述)
2. [核心工作原理](#核心工作原理)
3. [文件结构说明](#文件结构说明)
4. [如何使用不同方法刷题](#如何使用不同方法刷题)
5. [高级用法](#高级用法)

---

## 插件概述

LCPR (LeetCode Practice) 是一个用于在 Cursor/VSCode 中刷 LeetCode 题目的插件。它提供了：
- ✅ 本地编写和调试代码
- ✅ 自动测试用例执行
- ✅ 刷题进度跟踪（间隔重复算法）
- ✅ 多语言支持（Python, C++, Java等）

---

## 核心工作原理

### 1. 文件格式识别

插件通过识别文件中的特殊注释标记来理解代码结构：

```python
# @lc app=leetcode.cn id=560 lang=python3
# @lcpr version=
#
# [560] 和为 K 的子数组
```

**关键标记说明：**
- `@lc app=leetcode.cn`: 标识这是LeetCode题目
- `id=560`: 题目编号
- `lang=python3`: 编程语言
- `@lcpr case=start/end`: 测试用例标记

### 2. 测试用例解析

插件解析 `@lcpr case` 标记之间的内容作为测试用例：

```python
# @lcpr case=start
# [1,1,1]\n2\n
# @lcpr case=end
```

**格式说明：**
- 测试用例写在注释中
- `\n` 表示换行，分隔不同的输入参数
- 多个参数按顺序传递给函数

### 3. 代码执行流程

```
1. 用户编写代码（包含 @lcpr case 标记）
   ↓
2. 插件解析文件，提取：
   - 函数定义（Solution类）
   - 测试用例（@lcpr case之间的内容）
   ↓
3. 插件生成测试代码（临时文件）
   ↓
4. 执行测试代码，调用用户的Solution
   ↓
5. 显示测试结果（通过/失败）
```

### 4. 调试机制

插件会在代码顶部注入调试代码：

```python
# @lcpr-before-debug-begin
from python3problem560 import *
from typing import *
# @lcpr-before-debug-end
```

这允许调试器：
- 加载用户代码
- 执行测试用例
- 支持断点调试

---

## 文件结构说明

### 主要目录

```
.lcpr/
├── leetcode/
│   ├── cache/                    # 题目缓存目录
│   │   ├── 1.two-sum.algorithms.json
│   │   ├── 560.subarray-sum-equals-k.algorithms.json
│   │   └── ...
│   ├── stat.json                  # 刷题统计
│   └── user.json                  # 用户信息
├── .lcpr_data/
│   └── bricks.json                # 刷题进度（间隔重复）
├── 1.two-sum.py                   # 题目代码文件
├── 560.subarray-sum-equals-k.py
└── ...
```

### 关键文件详解

#### 1. `leetcode/cache/*.algorithms.json`

存储题目的完整信息：
- 题目描述（HTML格式）
- 多语言模板代码
- 测试用例
- 难度、标签等信息

#### 2. `.lcpr_data/bricks.json`

使用间隔重复算法跟踪刷题进度：

```json
{
  "all_bricks": {
    "1": {
      "submit_time": [1762159243],
      "type": 4,
      "review_day": [
        1762185600,  // 第1天复习
        1762444800,  // 第3天复习
        1762704000,  // 第7天复习
        ...
      ]
    }
  }
}
```

**间隔重复算法**：
- 根据遗忘曲线安排复习时间
- `review_day` 记录需要复习的日期
- 完成复习后更新到下一个间隔

#### 3. 题目代码文件

标准格式：

```python
# @lc app=leetcode.cn id=560 lang=python3
# @lcpr version=

# @lc code=start
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        # 你的代码
        pass
# @lc code=end

# @lcpr case=start
# [1,1,1]\n2\n
# @lcpr case=end
```

---

## 如何使用不同方法刷题

### 方法1：在同一文件中添加多个解法

**推荐做法**：在同一个文件中注释掉之前的解法，添加新解法：

```python
# @lc code=start
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        # ========== 方法1：暴力解法 O(n²) ==========
        # count = 0
        # for i in range(len(nums)):
        #     for j in range(i, len(nums)):
        #         if sum(nums[i:j+1]) == k:
        #             count += 1
        # return count
        
        # ========== 方法2：前缀和 + 哈希表 O(n) ==========
        from collections import defaultdict
        prefix_sum_count = defaultdict(int)
        prefix_sum_count[0] = 1
        
        prefix_sum = 0
        count = 0
        
        for num in nums:
            prefix_sum += num
            if prefix_sum - k in prefix_sum_count:
                count += prefix_sum_count[prefix_sum - k]
            prefix_sum_count[prefix_sum] += 1
        
        return count
# @lc code=end
```

**优点**：
- 方便对比不同解法
- 保留历史思路
- 文件组织清晰

### 方法2：创建多个文件（不同语言）

如果你想用不同语言实现：

```
560.subarray-sum-equals-k.py        # Python版本
560.subarray-sum-equals-k.cpp       # C++版本
560.subarray-sum-equals-k.java      # Java版本
```

每个文件都有独立的：
- 代码实现
- 测试用例
- 调试配置

### 方法3：创建多个文件（不同算法）

如果你想同时保留多个解法：

```
560.subarray-sum-equals-k.brute-force.py      # 暴力解法
560.subarray-sum-equals-k.prefix-sum.py       # 前缀和解法
560.subarray-sum-equals-k.sliding-window.py   # 滑动窗口解法
```

**命名建议**：
- `{题目编号}.{题目slug}.{解法名称}.{语言}`

### 方法4：使用不同的测试用例

在同一个文件中添加多个测试用例：

```python
# @lcpr case=start
# [1,1,1]\n2\n
# @lcpr case=end

# @lcpr case=start
# [1,2,3]\n3\n
# @lcpr case=end

# @lcpr case=start
# [1]\n1\n
# @lcpr case=end

# @lcpr case=start
# [1,-1,0]\n0\n
# @lcpr case=end
```

每个 `@lcpr case` 会生成一个独立的测试按钮。

### 方法5：使用Jupyter Notebook

对于需要逐步探索的题目，可以使用 Jupyter Notebook：

```
syntax/py/560.subarray-sum-equals-k.ipynb
```

**优点**：
- 可以逐步执行代码
- 可视化中间结果
- 方便记录思路和笔记

---

## 高级用法

### 1. 自定义测试用例

除了使用默认测试用例，你可以添加边界情况：

```python
# @lcpr case=start
# [1]\n1\n          # 单个元素
# @lcpr case=end

# @lcpr case=start
# [1,-1,0]\n0\n     # 包含0和负数
# @lcpr case=end

# @lcpr case=start
# [1,2,3,4,5]\n15\n # 全部元素
# @lcpr case=end
```

### 2. 调试技巧

参考 `LCPR_调试指南.md`：

1. **设置断点**：在关键位置设置断点
2. **点击Debug按钮**：在 `@lcpr case` 上方
3. **跳过插件代码**：按 F5 继续到你的代码
4. **查看变量**：在调试面板查看变量值

### 3. 导出题目为Markdown

使用 `export_lcpr_markdown.py`：

```bash
python3 export_lcpr_markdown.py --filter "560"
```

会在 `problems_markdown/` 目录生成题目描述。

### 4. 批量获取题目

使用 `fetch_all_cache_json.py` 可以批量获取题目信息到缓存目录。

### 5. 跟踪刷题进度

插件会自动更新 `bricks.json`：
- 记录提交时间
- 计算复习日期
- 支持间隔重复复习

---

## 总结

LCPR 插件通过以下方式实现：

1. **文件解析**：识别特殊注释标记（`@lc`, `@lcpr`）
2. **代码生成**：动态生成测试代码
3. **执行测试**：调用用户代码并验证结果
4. **进度跟踪**：使用间隔重复算法记录进度

**使用不同方法刷题的最佳实践**：

1. ✅ **推荐**：在同一文件中用注释保留多个解法
2. ✅ **多语言**：创建不同语言的文件
3. ✅ **多算法**：创建不同算法名称的文件
4. ✅ **探索性**：使用 Jupyter Notebook
5. ✅ **测试驱动**：添加多种测试用例

选择最适合你学习风格的方式！


